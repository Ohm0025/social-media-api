const { getContact } = require("./getContact");
const { getChat } = require("./getChat");
const { createChat } = require("./createChat");

module.exports = {
  getContact,
  getChat,
  createChat,
};
